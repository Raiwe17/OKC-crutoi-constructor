# ОКС - Конструктор сайтов

Очень крутой конструктор сайтов с визуальным редактором и системой узлов.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

## 📦 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

### Настройка GitHub Pages

1. **Создайте репозиторий на GitHub** (если еще не создан)

2. **Включите GitHub Pages в настройках репозитория:**
   - Перейдите в `Settings` → `Pages`
   - В разделе `Source` выберите `GitHub Actions`

3. **Настройте базовый путь (если нужно):**
   - Если ваш репозиторий называется не `Project_9.0-main`, обновите переменную `VITE_BASE_PATH` в файле `.github/workflows/deploy.yml`
   - Или установите переменную окружения `VITE_BASE_PATH` в настройках репозитория

4. **Загрузите код в репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   git push -u origin main
   ```

5. **Автоматический деплой:**
   - После каждого push в ветку `main` или `master` будет автоматически запускаться сборка и деплой
   - Проверить статус деплоя можно во вкладке `Actions` вашего репозитория
   - После успешного деплоя сайт будет доступен по адресу: `https://ВАШ_USERNAME.github.io/ВАШ_РЕПОЗИТОРИЙ/`

### Ручной деплой

Если нужно запустить деплой вручную:
- Перейдите во вкладку `Actions` вашего репозитория
- Выберите workflow `Deploy to GitHub Pages`
- Нажмите `Run workflow`

## 🔧 Переменные окружения

Проект использует следующие переменные окружения (опционально):

- `GEMINI_API_KEY` - API ключ для Gemini (если используется)
- `VITE_BASE_PATH` - Базовый путь для деплоя (по умолчанию `/`)

Для локальной разработки создайте файл `.env` в корне проекта:

```env
GEMINI_API_KEY=your_api_key_here
VITE_BASE_PATH=/
```

## 🔍 Решение проблем

### Проблемы с деплоем на GitHub Pages

1. **Сайт показывает 404 или пустую страницу:**
   - Убедитесь, что в настройках репозитория (`Settings` → `Pages`) выбран источник `GitHub Actions`
   - Проверьте, что базовый путь (`VITE_BASE_PATH`) в workflow соответствует имени вашего репозитория
   - Убедитесь, что workflow успешно завершился во вкладке `Actions`

2. **Ресурсы не загружаются (CSS/JS файлы):**
   - Проверьте, что `VITE_BASE_PATH` в `.github/workflows/deploy.yml` начинается и заканчивается с `/`
   - Например: `/repository-name/` (не `/repository-name` или `repository-name/`)

3. **Деплой не запускается автоматически:**
   - Убедитесь, что вы пушите в ветку `main` или `master`
   - Проверьте, что файл `.github/workflows/deploy.yml` находится в репозитории
   - Убедитесь, что у вас есть права на запись в репозиторий

### Локальная разработка

Если возникают ошибки TypeScript о недостающих типах:
```bash
npm install
```

Это установит все необходимые зависимости и типы.

## 📝 Технологии

- React 19
- TypeScript
- Vite
- Tailwind CSS
- Lucide React Icons

## 📄 Лицензия

Проект находится в разработке.